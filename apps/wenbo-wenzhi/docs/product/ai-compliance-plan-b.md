# 方案 B 说明 — 问知

问知不把生成式对话作为产品能力。C 端 `FeatureFlags.LLM_ENABLED=false`，不调用 `/ai/chat`。界面、按钮、应用市场资料不出现「AI」字样，使用「智慧问答」「智能解答」。

交付能力：

- 智慧问答：知识库关键词匹配，返回要点归纳、相关背景、延伸阅读
- 追问：用新问题再次匹配解答库
- 求知偏好：问卷 + 规则模板报告 + 主题匹配（工作 / 学习 / 生活）

取得安全评估报告与算法备案后，可再开启 LLM，并补齐生成内容标识。
