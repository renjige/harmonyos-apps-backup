// Auto-generated by runtimes/generate-element-catalog.mjs — do not edit manually

export class BadgeDef {
  id: string = ''
  title: string = ''
  summary: string = ''
  ruleKey: string = ''

  static make(id: string, title: string, summary: string, ruleKey: string): BadgeDef {
    const x = new BadgeDef()
    x.id = id
    x.title = title
    x.summary = summary
    x.ruleKey = ruleKey
    return x
  }
}

export class BadgeBank {
  static all(): BadgeDef[] {
    const list: BadgeDef[] = []
    list.push(BadgeDef.make('badge_first', '初次探索', '完成第一次元素档案阅读', 'explore_count_1'))
    list.push(BadgeDef.make('badge_ten', '十元素旅人', '累计探索 10 种不同元素', 'explore_count_10'))
    list.push(BadgeDef.make('badge_half', '半张周期表', '累计探索 59 种元素', 'explore_count_59'))
    list.push(BadgeDef.make('badge_full', '周期表大师', '探索完全部 118 种元素', 'explore_count_118'))
    list.push(BadgeDef.make('badge_challenge5', '挑战新手', '完成 5 道探索挑战', 'challenge_done_5'))
    list.push(BadgeDef.make('badge_challenge20', '挑战达人', '完成 20 道探索挑战', 'challenge_done_20'))
    list.push(BadgeDef.make('badge_perfect', '一次全对', '单次挑战测验全部答对', 'challenge_perfect_once'))
    list.push(BadgeDef.make('badge_metal', '金属猎人', '探索 20 种金属类元素', 'filter_metal_20'))
    list.push(BadgeDef.make('badge_noble', '稀有气体观察者', '探索全部 7 种稀有气体（He–Rn）', 'family_noble_all'))
    list.push(BadgeDef.make('badge_halogen', '卤素专家', '探索全部卤素 F、Cl、Br、I、At', 'family_halogen_all'))
    list.push(BadgeDef.make('badge_scenario3', '推演学徒', '完成 3 次科学推演', 'scenario_done_3'))
    list.push(BadgeDef.make('badge_favorite10', '收藏爱好者', '收藏 10 个元素或专题', 'favorite_count_10'))
    list.push(BadgeDef.make('badge_topic_all', '专题漫游', '浏览全部探索专题', 'topic_view_all'))
    list.push(BadgeDef.make('badge_streak7', '七日探索', '连续 7 天打开探索', 'streak_days_7'))
    return list
  }

  static find(id: string): BadgeDef | undefined {
    for (const b of BadgeBank.all()) {
      if (b.id === id) { return b }
    }
    return undefined
  }
}
