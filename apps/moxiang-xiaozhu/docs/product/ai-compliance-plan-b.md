# 方案 B — 智能能力说明（无生成式对话）

墨香小筑 C 端 **不调用** `/ai/chat`。`FeatureFlags.LLM_ENABLED = false`。

「智慧书房」匹配为：

1. 用户完成气质问卷（修身 / 处世 / 山水等）
2. `DiagnoseService` 按规则打分匹配书单主题
3. 输出模板报告 + 跳转对应书单详情

上架文案不主打「AI 文本生成」。取得《安全评估报告》前不得打开 LLM。
