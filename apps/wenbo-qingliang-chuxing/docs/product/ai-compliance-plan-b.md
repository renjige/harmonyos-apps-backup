# 清凉出行 · 智能能力合规（方案 B）

无《生成式人工智能服务安全评估报告》时：

- 客户端 `FeatureFlags.LLM_ENABLED = false`
- 不上架「AI 对话」「大模型」文案，使用「智能出行建议」「智慧路线」「出行自检」
- 出行自检 = 6 题问卷 + 规则模板报告 + 匹配已有清凉路线 ID
- 不调用 `POST /ai/chat`
- 取得报告后再评估是否开启 LLM，并补齐标识与备案

天气与路线结论均为模板/栏目数据，须在界面标明「出行参考，不替代气象台预警」。
